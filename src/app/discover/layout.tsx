import ConsumerLayout from '@/components/consumer/consumer-layout';

export default function Layout({ children }: { children: React.ReactNode }) {
  return <ConsumerLayout>{children}</ConsumerLayout>;
}
